
# Language setting
#language: python

language: java

branches:
  only:
    - feature/*
    - master
build:
  ci:
    - python --version
    - node --version
    - ruby -v

integrations:
    notifications:
      - integrationName: email
        type: email
        recipients:
          - manisha@shippable.com
