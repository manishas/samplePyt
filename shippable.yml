
# Language setting
#language: python

language: node_js

node_js:
  - "5.10.1"

branches:
  only:
    - feature/*
    - master
build:
  ci:
    - python --version
    - node --version
    - ruby -v

integrations:
    notifications:
      - integrationName: email
        type: email
        recipients:
          - manisha@shippable.com
